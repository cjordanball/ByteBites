const { ArrayAddition } = require('../../problems/medium/M008ArrayAddition');

describe('ArrayAddition', () => {
	it('returns a correct answer', () => {
		expect(ArrayAddition([4, 6, 23, 10, 1, 3])).toBe(true);
	});
});
